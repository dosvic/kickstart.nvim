# Neovim config

Personal Neovim configuration.

## Structure

- `init.lua`: small entrypoint
- `lua/config/`: core options, keymaps, autocmds, and lazy.nvim bootstrap
- `lua/plugins/`: plugin specs and plugin-specific configuration
